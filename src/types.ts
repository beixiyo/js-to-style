export interface Options {
  /**
   * 读取 js 文件路径
   */
  jsPath: string
  /**
   * 输出 css 文件路径
   */
  cssPath?: string
  /**
   * 输出 scss 文件路径
   */
  scssPath?: string
  /**
   * 输出 less 文件路径
   */
  lessPath?: string
}